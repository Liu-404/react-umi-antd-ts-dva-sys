import { Layout, Menu, Breadcrumb } from 'antd';

const { Header } = Layout;

export default function () {
    return (
        <Header className="header">
            <div style={{ color: '#fff' }}>头</div>
        </Header>
    );
}
